/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';
import enumFields from 'models/enum.model';

Joi.objectId = require('joi-objectid')(Joi);

export const createMessages = {
  body: Joi.object().keys({
    chatId: Joi.objectId(),
    senderId: Joi.object().unknown(true),
    message: Joi.string(),
    messageType: Joi.string().valid(...Object.values(enumFields.EnumMessageTypeOfMessages)),
    isRead: Joi.bool(),
  }),
};

export const updateMessages = {
  body: Joi.object().keys({
    chatId: Joi.objectId(),
    senderId: Joi.object().unknown(true),
    message: Joi.string(),
    messageType: Joi.string().valid(...Object.values(enumFields.EnumMessageTypeOfMessages)),
    isRead: Joi.bool(),
  }),
  params: Joi.object().keys({
    messagesId: Joi.objectId().required(),
  }),
};

export const getMessagesById = {
  params: Joi.object().keys({
    messagesId: Joi.objectId().required(),
  }),
};

export const deleteMessagesById = {
  params: Joi.object().keys({
    messagesId: Joi.objectId().required(),
  }),
};

export const getMessages = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedMessages = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
