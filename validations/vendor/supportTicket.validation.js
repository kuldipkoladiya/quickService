/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';
import enumFields from 'models/enum.model';

Joi.objectId = require('joi-objectid')(Joi);

export const createSupportTicket = {
  body: Joi.object().keys({
    userId: Joi.objectId(),
    bookingId: Joi.objectId(),
    issueType: Joi.string(),
    message: Joi.string(),
    images: Joi.array().items(Joi.string()),
    status: Joi.string().valid(...Object.values(enumFields.EnumStatusOfSupportTicket)),
    adminReply: Joi.string(),
  }),
};

export const updateSupportTicket = {
  body: Joi.object().keys({
    userId: Joi.objectId(),
    bookingId: Joi.objectId(),
    issueType: Joi.string(),
    message: Joi.string(),
    images: Joi.array().items(Joi.string()),
    status: Joi.string().valid(...Object.values(enumFields.EnumStatusOfSupportTicket)),
    adminReply: Joi.string(),
  }),
  params: Joi.object().keys({
    supportTicketId: Joi.objectId().required(),
  }),
};

export const getSupportTicketById = {
  params: Joi.object().keys({
    supportTicketId: Joi.objectId().required(),
  }),
};

export const deleteSupportTicketById = {
  params: Joi.object().keys({
    supportTicketId: Joi.objectId().required(),
  }),
};

export const getSupportTicket = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedSupportTicket = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
