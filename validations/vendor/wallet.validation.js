/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';
import enumFields from 'models/enum.model';

Joi.objectId = require('joi-objectid')(Joi);

export const createWallet = {
  body: Joi.object().keys({
    userId: Joi.objectId(),
    role: Joi.string().valid(...Object.values(enumFields.EnumRoleOfWallet)),
    totalEarnings: Joi.number().integer(),
    totalWithdrawn: Joi.number().integer(),
  }),
};

export const updateWallet = {
  body: Joi.object().keys({
    userId: Joi.objectId(),
    role: Joi.string().valid(...Object.values(enumFields.EnumRoleOfWallet)),
    totalEarnings: Joi.number().integer(),
    totalWithdrawn: Joi.number().integer(),
  }),
  params: Joi.object().keys({
    walletId: Joi.objectId().required(),
  }),
};

export const getWalletById = {
  params: Joi.object().keys({
    walletId: Joi.objectId().required(),
  }),
};

export const deleteWalletById = {
  params: Joi.object().keys({
    walletId: Joi.objectId().required(),
  }),
};

export const getWallet = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedWallet = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
