/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createBookingCoupons = {
  body: Joi.object().keys({
    bookingId: Joi.objectId(),
    couponId: Joi.objectId(),
    discountAmount: Joi.number().integer(),
  }),
};

export const updateBookingCoupons = {
  body: Joi.object().keys({
    bookingId: Joi.objectId(),
    couponId: Joi.objectId(),
    discountAmount: Joi.number().integer(),
  }),
  params: Joi.object().keys({
    bookingCouponsId: Joi.objectId().required(),
  }),
};

export const getBookingCouponsById = {
  params: Joi.object().keys({
    bookingCouponsId: Joi.objectId().required(),
  }),
};

export const deleteBookingCouponsById = {
  params: Joi.object().keys({
    bookingCouponsId: Joi.objectId().required(),
  }),
};

export const getBookingCoupons = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedBookingCoupons = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
