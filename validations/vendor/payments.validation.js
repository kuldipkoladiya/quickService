/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';
import enumFields from 'models/enum.model';

Joi.objectId = require('joi-objectid')(Joi);

export const createPayments = {
  body: Joi.object().keys({
    bookingId: Joi.objectId(),
    customerId: Joi.objectId(),
    vendorId: Joi.objectId(),
    amount: Joi.number().integer(),
    transactionId: Joi.string(),
    paymentGateway: Joi.string().valid(...Object.values(enumFields.EnumPaymentGatewayOfPayments)),
    paymentStatus: Joi.string().valid(...Object.values(enumFields.EnumPaymentStatusOfPayments)),
    adminCommission: Joi.number().integer(),
    vendorAmount: Joi.number().integer(),
    refundStatus: Joi.string().valid(...Object.values(enumFields.EnumRefundStatusOfPayments)),
  }),
};

export const updatePayments = {
  body: Joi.object().keys({
    bookingId: Joi.objectId(),
    customerId: Joi.objectId(),
    vendorId: Joi.objectId(),
    amount: Joi.number().integer(),
    transactionId: Joi.string(),
    paymentGateway: Joi.string().valid(...Object.values(enumFields.EnumPaymentGatewayOfPayments)),
    paymentStatus: Joi.string().valid(...Object.values(enumFields.EnumPaymentStatusOfPayments)),
    adminCommission: Joi.number().integer(),
    vendorAmount: Joi.number().integer(),
    refundStatus: Joi.string().valid(...Object.values(enumFields.EnumRefundStatusOfPayments)),
  }),
  params: Joi.object().keys({
    paymentsId: Joi.objectId().required(),
  }),
};

export const getPaymentsById = {
  params: Joi.object().keys({
    paymentsId: Joi.objectId().required(),
  }),
};

export const deletePaymentsById = {
  params: Joi.object().keys({
    paymentsId: Joi.objectId().required(),
  }),
};

export const getPayments = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedPayments = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
