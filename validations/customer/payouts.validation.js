/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';
import enumFields from 'models/enum.model';

Joi.objectId = require('joi-objectid')(Joi);

export const createPayouts = {
  body: Joi.object().keys({
    vendorId: Joi.objectId(),
    walletId: Joi.objectId(),
    amount: Joi.number().integer(),
    utrTransactionId: Joi.string(),
    status: Joi.string().valid(...Object.values(enumFields.EnumStatusOfPayouts)),
    requestedAt: Joi.date(),
    paidAt: Joi.string(),
  }),
};

export const updatePayouts = {
  body: Joi.object().keys({
    vendorId: Joi.objectId(),
    walletId: Joi.objectId(),
    amount: Joi.number().integer(),
    utrTransactionId: Joi.string(),
    status: Joi.string().valid(...Object.values(enumFields.EnumStatusOfPayouts)),
    requestedAt: Joi.date(),
    paidAt: Joi.string(),
  }),
  params: Joi.object().keys({
    payoutsId: Joi.objectId().required(),
  }),
};

export const getPayoutsById = {
  params: Joi.object().keys({
    payoutsId: Joi.objectId().required(),
  }),
};

export const deletePayoutsById = {
  params: Joi.object().keys({
    payoutsId: Joi.objectId().required(),
  }),
};

export const getPayouts = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedPayouts = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
