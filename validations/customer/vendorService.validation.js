/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createVendorService = {
  body: Joi.object().keys({
    vendorId: Joi.objectId(),
    serviceId: Joi.objectId(),
    categoryId: Joi.objectId().optional(),
    price: Joi.number().integer(),
    isAvailable: Joi.bool(),
  }),
};

export const updateVendorService = {
  body: Joi.object().keys({
    vendorId: Joi.objectId(),
    serviceId: Joi.objectId(),
    categoryId: Joi.objectId().optional(),
    price: Joi.number().integer(),
    isAvailable: Joi.bool(),
  }),
  params: Joi.object().keys({
    vendorServiceId: Joi.objectId().required(),
  }),
};

export const getVendorServiceById = {
  params: Joi.object().keys({
    vendorServiceId: Joi.objectId().required(),
  }),
};

export const deleteVendorServiceById = {
  params: Joi.object().keys({
    vendorServiceId: Joi.objectId().required(),
  }),
};

export const getVendorService = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedVendorService = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};

export const getVendorServicesByCategory = {
  params: Joi.object().keys({
    categoryId: Joi.objectId().required(),
  }),
  query: Joi.object()
    .keys({
      latitude: Joi.number().optional(),
      longitude: Joi.number().optional(),
      page: Joi.number().integer().optional(),
      limit: Joi.number().integer().optional(),
    })
    .unknown(true),
};
