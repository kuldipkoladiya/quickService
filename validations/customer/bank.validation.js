/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';
import enumFields from 'models/enum.model';

Joi.objectId = require('joi-objectid')(Joi);

export const createBank = {
  body: Joi.object().keys({
    vendorId: Joi.objectId(),
    accountHoldername: Joi.string(),
    bankName: Joi.string(),
    accountNumber: Joi.string(),
    ifscCode: Joi.string(),
    branchName: Joi.string(),
    upiId: Joi.string(),
    accountType: Joi.string().valid(...Object.values(enumFields.EnumAccountTypeOfBank)),
    isDefault: Joi.bool(),
    verificationStatus: Joi.string().valid(...Object.values(enumFields.EnumVerificationStatusOfBank)),
  }),
};

export const updateBank = {
  body: Joi.object().keys({
    vendorId: Joi.objectId(),
    accountHoldername: Joi.string(),
    bankName: Joi.string(),
    accountNumber: Joi.string(),
    ifscCode: Joi.string(),
    branchName: Joi.string(),
    upiId: Joi.string(),
    accountType: Joi.string().valid(...Object.values(enumFields.EnumAccountTypeOfBank)),
    isDefault: Joi.bool(),
    verificationStatus: Joi.string().valid(...Object.values(enumFields.EnumVerificationStatusOfBank)),
  }),
  params: Joi.object().keys({
    bankId: Joi.objectId().required(),
  }),
};

export const getBankById = {
  params: Joi.object().keys({
    bankId: Joi.objectId().required(),
  }),
};

export const deleteBankById = {
  params: Joi.object().keys({
    bankId: Joi.objectId().required(),
  }),
};

export const getBank = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedBank = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
