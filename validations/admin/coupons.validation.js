/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createCoupons = {
  body: Joi.object().keys({
    code: Joi.string(),
    title: Joi.string(),
    discountType: Joi.string(),
    discountValue: Joi.string(),
    minOrderAmount: Joi.string(),
    maxDiscount: Joi.string(),
    startDate: Joi.date(),
    endDate: Joi.date(),
    usageLimit: Joi.string(),
    isActive: Joi.bool(),
  }),
};

export const updateCoupons = {
  body: Joi.object().keys({
    code: Joi.string(),
    title: Joi.string(),
    discountType: Joi.string(),
    discountValue: Joi.string(),
    minOrderAmount: Joi.string(),
    maxDiscount: Joi.string(),
    startDate: Joi.date(),
    endDate: Joi.date(),
    usageLimit: Joi.string(),
    isActive: Joi.bool(),
  }),
  params: Joi.object().keys({
    couponsId: Joi.objectId().required(),
  }),
};

export const getCouponsById = {
  params: Joi.object().keys({
    couponsId: Joi.objectId().required(),
  }),
};

export const deleteCouponsById = {
  params: Joi.object().keys({
    couponsId: Joi.objectId().required(),
  }),
};

export const getCoupons = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedCoupons = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
