/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createNotifications = {
  body: Joi.object().keys({
    senderId: Joi.object().unknown(true),
    receiverId: Joi.object().unknown(true),
    title: Joi.string(),
    message: Joi.string(),
    notificationType: Joi.string(),
    related: Joi.string(),
    isRead: Joi.bool(),
  }),
};

export const updateNotifications = {
  body: Joi.object().keys({
    senderId: Joi.object().unknown(true),
    receiverId: Joi.object().unknown(true),
    title: Joi.string(),
    message: Joi.string(),
    notificationType: Joi.string(),
    related: Joi.string(),
    isRead: Joi.bool(),
  }),
  params: Joi.object().keys({
    notificationsId: Joi.objectId().required(),
  }),
};

export const getNotificationsById = {
  params: Joi.object().keys({
    notificationsId: Joi.objectId().required(),
  }),
};

export const deleteNotificationsById = {
  params: Joi.object().keys({
    notificationsId: Joi.objectId().required(),
  }),
};

export const getNotifications = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedNotifications = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
