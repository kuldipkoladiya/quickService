/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';
import enumFields from 'models/enum.model';

Joi.objectId = require('joi-objectid')(Joi);

export const createVendorKyc = {
  body: Joi.object().keys({
    vendorId: Joi.objectId(),
    documentType: Joi.string(),
    documentFront: Joi.string(),
    documentBack: Joi.string(),
    selfieVideo: Joi.string(),
    status: Joi.string().valid(...Object.values(enumFields.EnumStatusOfVendorKyc)),
    rejectedReason: Joi.string(),
    verifiedBy: Joi.object().unknown(true),
  }),
};

export const updateVendorKyc = {
  body: Joi.object().keys({
    vendorId: Joi.objectId(),
    documentType: Joi.string(),
    documentFront: Joi.string(),
    documentBack: Joi.string(),
    selfieVideo: Joi.string(),
    status: Joi.string().valid(...Object.values(enumFields.EnumStatusOfVendorKyc)),
    rejectedReason: Joi.string(),
    verifiedBy: Joi.object().unknown(true),
  }),
  params: Joi.object().keys({
    vendorKycId: Joi.objectId().required(),
  }),
};

export const getVendorKycById = {
  params: Joi.object().keys({
    vendorKycId: Joi.objectId().required(),
  }),
};

export const deleteVendorKycById = {
  params: Joi.object().keys({
    vendorKycId: Joi.objectId().required(),
  }),
};

export const getVendorKyc = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedVendorKyc = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
