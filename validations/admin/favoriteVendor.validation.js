/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createFavoriteVendor = {
  body: Joi.object().keys({
    userId: Joi.objectId(),
    vendorId: Joi.objectId(),
  }),
};

export const updateFavoriteVendor = {
  body: Joi.object().keys({
    userId: Joi.objectId(),
    vendorId: Joi.objectId(),
  }),
  params: Joi.object().keys({
    favoriteVendorId: Joi.objectId().required(),
  }),
};

export const getFavoriteVendorById = {
  params: Joi.object().keys({
    favoriteVendorId: Joi.objectId().required(),
  }),
};

export const deleteFavoriteVendorById = {
  params: Joi.object().keys({
    favoriteVendorId: Joi.objectId().required(),
  }),
};

export const getFavoriteVendor = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedFavoriteVendor = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
