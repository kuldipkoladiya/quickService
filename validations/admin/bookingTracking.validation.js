/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';
import enumFields from 'models/enum.model';

Joi.objectId = require('joi-objectid')(Joi);

const locationSchema = Joi.object().keys({
  type: Joi.string().equal('Point').required(),
  coordinates: Joi.array().required(),
});
export const createBookingTracking = {
  body: Joi.object().keys({
    bookingId: Joi.objectId(),
    status: Joi.string().valid(...Object.values(enumFields.EnumStatusOfBookingTracking)),
    location: locationSchema,
    note: Joi.string(),
  }),
};

export const updateBookingTracking = {
  body: Joi.object().keys({
    bookingId: Joi.objectId(),
    status: Joi.string().valid(...Object.values(enumFields.EnumStatusOfBookingTracking)),
    location: locationSchema,
    note: Joi.string(),
  }),
  params: Joi.object().keys({
    bookingTrackingId: Joi.objectId().required(),
  }),
};

export const getBookingTrackingById = {
  params: Joi.object().keys({
    bookingTrackingId: Joi.objectId().required(),
  }),
};

export const deleteBookingTrackingById = {
  params: Joi.object().keys({
    bookingTrackingId: Joi.objectId().required(),
  }),
};

export const getBookingTracking = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedBookingTracking = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
