/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { bankService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getBank = catchAsync(async (req, res) => {
  const { bankId } = req.params;
  const filter = {
    _id: bankId,
  };
  const options = {};
  const bank = await bankService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: bank });
});

export const listBank = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const bank = await bankService.getBankList(filter, options);
  return res.status(httpStatus.OK).send({ results: bank });
});

export const paginateBank = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const bank = await bankService.getBankListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: bank });
});

export const createBank = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const bank = await bankService.createBank(body, options);
  return res.status(httpStatus.CREATED).send({ results: bank });
});

export const updateBank = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { bankId } = req.params;
  const filter = {
    _id: bankId,
  };
  const options = { new: true };
  const bank = await bankService.updateBank(filter, body, options);
  return res.status(httpStatus.OK).send({ results: bank });
});

export const removeBank = catchAsync(async (req, res) => {
  const { bankId } = req.params;
  const filter = {
    _id: bankId,
  };
  const bank = await bankService.removeBank(filter);
  return res.status(httpStatus.OK).send({ results: bank });
});
