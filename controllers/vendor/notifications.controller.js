/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { notificationsService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getNotifications = catchAsync(async (req, res) => {
  const { notificationsId } = req.params;
  const filter = {
    _id: notificationsId,
  };
  const options = {};
  const notifications = await notificationsService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: notifications });
});

export const listNotifications = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const notifications = await notificationsService.getNotificationsList(filter, options);
  return res.status(httpStatus.OK).send({ results: notifications });
});

export const paginateNotifications = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const notifications = await notificationsService.getNotificationsListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: notifications });
});

export const createNotifications = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const notifications = await notificationsService.createNotifications(body, options);
  return res.status(httpStatus.CREATED).send({ results: notifications });
});

export const updateNotifications = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { notificationsId } = req.params;
  const filter = {
    _id: notificationsId,
  };
  const options = { new: true };
  const notifications = await notificationsService.updateNotifications(filter, body, options);
  return res.status(httpStatus.OK).send({ results: notifications });
});

export const removeNotifications = catchAsync(async (req, res) => {
  const { notificationsId } = req.params;
  const filter = {
    _id: notificationsId,
  };
  const notifications = await notificationsService.removeNotifications(filter);
  return res.status(httpStatus.OK).send({ results: notifications });
});
