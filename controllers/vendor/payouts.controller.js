/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { payoutsService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getPayouts = catchAsync(async (req, res) => {
  const { payoutsId } = req.params;
  const filter = {
    _id: payoutsId,
  };
  const options = {};
  const payouts = await payoutsService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: payouts });
});

export const listPayouts = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const payouts = await payoutsService.getPayoutsList(filter, options);
  return res.status(httpStatus.OK).send({ results: payouts });
});

export const paginatePayouts = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const payouts = await payoutsService.getPayoutsListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: payouts });
});

export const createPayouts = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const payouts = await payoutsService.createPayouts(body, options);
  return res.status(httpStatus.CREATED).send({ results: payouts });
});

export const updatePayouts = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { payoutsId } = req.params;
  const filter = {
    _id: payoutsId,
  };
  const options = { new: true };
  const payouts = await payoutsService.updatePayouts(filter, body, options);
  return res.status(httpStatus.OK).send({ results: payouts });
});

export const removePayouts = catchAsync(async (req, res) => {
  const { payoutsId } = req.params;
  const filter = {
    _id: payoutsId,
  };
  const payouts = await payoutsService.removePayouts(filter);
  return res.status(httpStatus.OK).send({ results: payouts });
});
