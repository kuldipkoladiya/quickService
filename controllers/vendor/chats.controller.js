/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { chatsService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getChats = catchAsync(async (req, res) => {
  const { chatsId } = req.params;
  const filter = {
    _id: chatsId,
  };
  const options = {};
  const chats = await chatsService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: chats });
});

export const listChats = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const chats = await chatsService.getChatsList(filter, options);
  return res.status(httpStatus.OK).send({ results: chats });
});

export const paginateChats = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const chats = await chatsService.getChatsListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: chats });
});

export const createChats = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const chats = await chatsService.createChats(body, options);
  return res.status(httpStatus.CREATED).send({ results: chats });
});

export const updateChats = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { chatsId } = req.params;
  const filter = {
    _id: chatsId,
  };
  const options = { new: true };
  const chats = await chatsService.updateChats(filter, body, options);
  return res.status(httpStatus.OK).send({ results: chats });
});

export const removeChats = catchAsync(async (req, res) => {
  const { chatsId } = req.params;
  const filter = {
    _id: chatsId,
  };
  const chats = await chatsService.removeChats(filter);
  return res.status(httpStatus.OK).send({ results: chats });
});
