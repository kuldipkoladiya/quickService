/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { paymentsService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getPayments = catchAsync(async (req, res) => {
  const { paymentsId } = req.params;
  const filter = {
    _id: paymentsId,
  };
  const options = {};
  const payments = await paymentsService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: payments });
});

export const listPayments = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const payments = await paymentsService.getPaymentsList(filter, options);
  return res.status(httpStatus.OK).send({ results: payments });
});

export const paginatePayments = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const payments = await paymentsService.getPaymentsListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: payments });
});

export const createPayments = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const payments = await paymentsService.createPayments(body, options);
  return res.status(httpStatus.CREATED).send({ results: payments });
});

export const updatePayments = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { paymentsId } = req.params;
  const filter = {
    _id: paymentsId,
  };
  const options = { new: true };
  const payments = await paymentsService.updatePayments(filter, body, options);
  return res.status(httpStatus.OK).send({ results: payments });
});

export const removePayments = catchAsync(async (req, res) => {
  const { paymentsId } = req.params;
  const filter = {
    _id: paymentsId,
  };
  const payments = await paymentsService.removePayments(filter);
  return res.status(httpStatus.OK).send({ results: payments });
});
