/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { couponsService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getCoupons = catchAsync(async (req, res) => {
  const { couponsId } = req.params;
  const filter = {
    _id: couponsId,
  };
  const options = {};
  const coupons = await couponsService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: coupons });
});

export const listCoupons = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const coupons = await couponsService.getCouponsList(filter, options);
  return res.status(httpStatus.OK).send({ results: coupons });
});

export const paginateCoupons = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const coupons = await couponsService.getCouponsListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: coupons });
});

export const createCoupons = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const coupons = await couponsService.createCoupons(body, options);
  return res.status(httpStatus.CREATED).send({ results: coupons });
});

export const updateCoupons = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { couponsId } = req.params;
  const filter = {
    _id: couponsId,
  };
  const options = { new: true };
  const coupons = await couponsService.updateCoupons(filter, body, options);
  return res.status(httpStatus.OK).send({ results: coupons });
});

export const removeCoupons = catchAsync(async (req, res) => {
  const { couponsId } = req.params;
  const filter = {
    _id: couponsId,
  };
  const coupons = await couponsService.removeCoupons(filter);
  return res.status(httpStatus.OK).send({ results: coupons });
});
