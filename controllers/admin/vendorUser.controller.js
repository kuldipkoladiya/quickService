/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { vendorUserService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getVendorUser = catchAsync(async (req, res) => {
  const { vendorUserId } = req.params;
  const filter = {
    _id: vendorUserId,
  };
  const options = {};
  const vendorUser = await vendorUserService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: vendorUser });
});

export const listVendorUser = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const vendorUser = await vendorUserService.getVendorUserList(filter, options);
  return res.status(httpStatus.OK).send({ results: vendorUser });
});

export const paginateVendorUser = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const vendorUser = await vendorUserService.getVendorUserListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: vendorUser });
});

export const createVendorUser = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const vendorUser = await vendorUserService.createVendorUser(body, options);
  return res.status(httpStatus.CREATED).send({ results: vendorUser });
});

export const updateVendorUser = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { vendorUserId } = req.params;
  const filter = {
    _id: vendorUserId,
  };
  const options = { new: true };
  const vendorUser = await vendorUserService.updateVendorUser(filter, body, options);
  return res.status(httpStatus.OK).send({ results: vendorUser });
});

export const removeVendorUser = catchAsync(async (req, res) => {
  const { vendorUserId } = req.params;
  const filter = {
    _id: vendorUserId,
  };
  const vendorUser = await vendorUserService.removeVendorUser(filter);
  return res.status(httpStatus.OK).send({ results: vendorUser });
});
