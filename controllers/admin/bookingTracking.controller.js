/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { bookingTrackingService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getBookingTracking = catchAsync(async (req, res) => {
  const { bookingTrackingId } = req.params;
  const filter = {
    _id: bookingTrackingId,
  };
  const options = {};
  const bookingTracking = await bookingTrackingService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: bookingTracking });
});

export const listBookingTracking = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const bookingTracking = await bookingTrackingService.getBookingTrackingList(filter, options);
  return res.status(httpStatus.OK).send({ results: bookingTracking });
});

export const paginateBookingTracking = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const bookingTracking = await bookingTrackingService.getBookingTrackingListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: bookingTracking });
});

export const createBookingTracking = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const bookingTracking = await bookingTrackingService.createBookingTracking(body, options);
  return res.status(httpStatus.CREATED).send({ results: bookingTracking });
});

export const updateBookingTracking = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { bookingTrackingId } = req.params;
  const filter = {
    _id: bookingTrackingId,
  };
  const options = { new: true };
  const bookingTracking = await bookingTrackingService.updateBookingTracking(filter, body, options);
  return res.status(httpStatus.OK).send({ results: bookingTracking });
});

export const removeBookingTracking = catchAsync(async (req, res) => {
  const { bookingTrackingId } = req.params;
  const filter = {
    _id: bookingTrackingId,
  };
  const bookingTracking = await bookingTrackingService.removeBookingTracking(filter);
  return res.status(httpStatus.OK).send({ results: bookingTracking });
});
