/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { vendorKycService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getVendorKyc = catchAsync(async (req, res) => {
  const { vendorKycId } = req.params;
  const filter = {
    _id: vendorKycId,
  };
  const options = {};
  const vendorKyc = await vendorKycService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: vendorKyc });
});

export const listVendorKyc = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const vendorKyc = await vendorKycService.getVendorKycList(filter, options);
  return res.status(httpStatus.OK).send({ results: vendorKyc });
});

export const paginateVendorKyc = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const vendorKyc = await vendorKycService.getVendorKycListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: vendorKyc });
});

export const createVendorKyc = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const vendorKyc = await vendorKycService.createVendorKyc(body, options);
  return res.status(httpStatus.CREATED).send({ results: vendorKyc });
});

export const updateVendorKyc = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { vendorKycId } = req.params;
  const filter = {
    _id: vendorKycId,
  };
  const options = { new: true };
  const vendorKyc = await vendorKycService.updateVendorKyc(filter, body, options);
  return res.status(httpStatus.OK).send({ results: vendorKyc });
});

export const removeVendorKyc = catchAsync(async (req, res) => {
  const { vendorKycId } = req.params;
  const filter = {
    _id: vendorKycId,
  };
  const vendorKyc = await vendorKycService.removeVendorKyc(filter);
  return res.status(httpStatus.OK).send({ results: vendorKyc });
});
