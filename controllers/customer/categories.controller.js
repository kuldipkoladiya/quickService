/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { categoriesService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getCategories = catchAsync(async (req, res) => {
  const { categoriesId } = req.params;
  const filter = {
    _id: categoriesId,
  };
  const options = {};
  const categories = await categoriesService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: categories });
});

export const listCategories = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const categories = await categoriesService.getCategoriesList(filter, options);
  return res.status(httpStatus.OK).send({ results: categories });
});

export const paginateCategories = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const categories = await categoriesService.getCategoriesListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: categories });
});

export const createCategories = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const categories = await categoriesService.createCategories(body, options);
  return res.status(httpStatus.CREATED).send({ results: categories });
});

export const updateCategories = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { categoriesId } = req.params;
  const filter = {
    _id: categoriesId,
  };
  const options = { new: true };
  const categories = await categoriesService.updateCategories(filter, body, options);
  return res.status(httpStatus.OK).send({ results: categories });
});

export const removeCategories = catchAsync(async (req, res) => {
  const { categoriesId } = req.params;
  const filter = {
    _id: categoriesId,
  };
  const categories = await categoriesService.removeCategories(filter);
  return res.status(httpStatus.OK).send({ results: categories });
});
