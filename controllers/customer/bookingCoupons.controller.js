/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { bookingCouponsService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getBookingCoupons = catchAsync(async (req, res) => {
  const { bookingCouponsId } = req.params;
  const filter = {
    _id: bookingCouponsId,
  };
  const options = {};
  const bookingCoupons = await bookingCouponsService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: bookingCoupons });
});

export const listBookingCoupons = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const bookingCoupons = await bookingCouponsService.getBookingCouponsList(filter, options);
  return res.status(httpStatus.OK).send({ results: bookingCoupons });
});

export const paginateBookingCoupons = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const bookingCoupons = await bookingCouponsService.getBookingCouponsListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: bookingCoupons });
});

export const createBookingCoupons = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const bookingCoupons = await bookingCouponsService.createBookingCoupons(body, options);
  return res.status(httpStatus.CREATED).send({ results: bookingCoupons });
});

export const updateBookingCoupons = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { bookingCouponsId } = req.params;
  const filter = {
    _id: bookingCouponsId,
  };
  const options = { new: true };
  const bookingCoupons = await bookingCouponsService.updateBookingCoupons(filter, body, options);
  return res.status(httpStatus.OK).send({ results: bookingCoupons });
});

export const removeBookingCoupons = catchAsync(async (req, res) => {
  const { bookingCouponsId } = req.params;
  const filter = {
    _id: bookingCouponsId,
  };
  const bookingCoupons = await bookingCouponsService.removeBookingCoupons(filter);
  return res.status(httpStatus.OK).send({ results: bookingCoupons });
});
