/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { supportTicketService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getSupportTicket = catchAsync(async (req, res) => {
  const { supportTicketId } = req.params;
  const filter = {
    _id: supportTicketId,
  };
  const options = {};
  const supportTicket = await supportTicketService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: supportTicket });
});

export const listSupportTicket = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const supportTicket = await supportTicketService.getSupportTicketList(filter, options);
  return res.status(httpStatus.OK).send({ results: supportTicket });
});

export const paginateSupportTicket = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const supportTicket = await supportTicketService.getSupportTicketListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: supportTicket });
});

export const createSupportTicket = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const supportTicket = await supportTicketService.createSupportTicket(body, options);
  return res.status(httpStatus.CREATED).send({ results: supportTicket });
});

export const updateSupportTicket = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { supportTicketId } = req.params;
  const filter = {
    _id: supportTicketId,
  };
  const options = { new: true };
  const supportTicket = await supportTicketService.updateSupportTicket(filter, body, options);
  return res.status(httpStatus.OK).send({ results: supportTicket });
});

export const removeSupportTicket = catchAsync(async (req, res) => {
  const { supportTicketId } = req.params;
  const filter = {
    _id: supportTicketId,
  };
  const supportTicket = await supportTicketService.removeSupportTicket(filter);
  return res.status(httpStatus.OK).send({ results: supportTicket });
});
